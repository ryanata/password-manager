import { Modal, Title, TextInput, Group, Button } from '@mantine/core';
import { useForm } from '@mantine/hooks';

const AccountModal = () => {
    return ( 
        <>
        </>
     );
}
 
export default AccountModal;